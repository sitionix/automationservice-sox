tables:
  agents:
    columns:
      agent_id: uuid
      user_id: bigint
      name: varchar(60)
      description: varchar(160)
      status: varchar(32)
      created_at: timestamptz
      updated_at: timestamptz
